apiVersion: v1
kind: Secret
metadata:
  name: gitlab-oauth-secret
  namespace: gitlab
type: Opaque
stringData:
  # Replace with your Google OAuth credentials from Google Developer Console
  GOOGLE_CLIENT_ID: "YOUR_GOOGLE_CLIENT_ID.apps.googleusercontent.com"
  GOOGLE_CLIENT_SECRET: "YOUR_GOOGLE_CLIENT_SECRET"
  # Optional: Initial root password (must not contain common words like admin or password)
  GITLAB_ROOT_PASSWORD: "Gl_Homelab_2026_xK3s#9"
